---
phase: 05-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PLBeast/PLBeast.lua
  - PLBeast/Locales/enUS.lua
autonomous: false
requirements: [CFG-01, CFG-02, CFG-03, CFG-04]
must_haves:
  truths:
    - "Typing bare /plbeast opens the options frame; typing it again hides it (toggle)"
    - "/plbeast typed during combat does not open the frame; it opens automatically when combat ends"
    - "The frame contains a width slider, a height slider, and a border-thickness slider"
    - "The frame contains a Lock position checkbox seeded from current DB state"
    - "Width and height are independent (no sync coupling); moving either slider resizes only that dimension live with no /reload"
    - "Moving the border-thickness slider changes the icon border live with no /reload"
    - "debug and reset subcommands still work unchanged"
  artifacts:
    - path: "PLBeast/PLBeast.lua"
      provides: "ToggleOptions() builder, optionsFrame upvalue, pendingOptionsOpenAfterCombat flag, PLAYER_REGEN_ENABLED handler, CreateSlider/CreateCheckbox helpers, SetSliderEnabled, bare-command frame routing"
      contains: "BasicFrameTemplateWithInset"
    - path: "PLBeast/Locales/enUS.lua"
      provides: "New CFG UI label strings"
      contains: "Width"
  key_links:
    - from: "SlashCmdList[\"PLBEAST\"]"
      to: "ToggleOptions()"
      via: "bare-command (msg == \"\") branch calls ToggleOptions()"
      pattern: "msg == \"\""
    - from: "width/height sliders"
      to: "SetIconSize()"
      via: "OnValueChanged setter calls SetIconSize(value, DB.height) / SetIconSize(DB.width, value)"
      pattern: "SetIconSize"
    - from: "border-thickness slider"
      to: "ApplyIconSettings()"
      via: "OnValueChanged writes DB.borderThickness then calls ApplyIconSettings()"
      pattern: "ApplyIconSettings"
    - from: "PLAYER_REGEN_ENABLED handler"
      to: "ToggleOptions()"
      via: "deferred open when pendingOptionsOpenAfterCombat is true"
      pattern: "pendingOptionsOpenAfterCombat"
---

<objective>
Deliver the lightweight slash-command options frame for PLBeast: bare `/plbeast` opens (toggles) a `BasicFrameTemplateWithInset` window containing three integer sliders (width, height, border thickness) and two checkboxes (Lock position, Sync width & height), all wired to the existing live-apply setters. Opening is combat-gated and deferred to `PLAYER_REGEN_ENABLED` (CFG-04). The color picker swatch is intentionally NOT in this plan — it is the net-new, version-sensitive widget delivered in Plan 05-02.

This is the thin end-to-end vertical slice for Phase 5: after this plan, a player can open the frame and change icon size, border thickness, drag-lock, and sync live without `/reload`.

Implements D-03, D-04 (standalone slash-only frame — explicitly NOT registered in Blizzard Settings, that is v2 CFG-08), D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12 (color-picker D-01/D-02 deferred to 05-02).

Purpose: Give the player a real control surface over the Phase 4 settings that previously could only be changed by hand-editing SavedVariables.
Output: `ToggleOptions()` + helpers in `PLBeast.lua`; new locale strings in `enUS.lua`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-configuration/05-CONTEXT.md
@.planning/phases/05-configuration/05-UI-SPEC.md
@.planning/phases/04-icon-ui/04-VERIFICATION.md
</context>

## Phase Goal

**As a** Pack Leader hunter, **I want to** open a small options window with `/plbeast` and adjust the icon's size, border, lock, and sync settings live, **so that** I can position and style the next-beast icon without hand-editing SavedVariables or reloading.

## Artifacts this phase produces

New symbols introduced across Phase 5 (this plan unless noted "[05-02]"):

- `optionsFrame` — module-level local holding the lazily-created options frame reference
- `pendingOptionsOpenAfterCombat` — module-level boolean flag for combat-deferred open
- `ToggleOptions()` — local function that lazy-creates and toggles the `"PLBeastOptionsFrame"` frame (built from `BasicFrameTemplateWithInset`)
- `CreateSlider(parent, label, minVal, maxVal, step, getValue, setValue, yOffset)` — trimmed slider factory (no editBox)
- `CreateCheckbox(parent, label, getValue, setValue, xOffset, yOffset)` — checkbox factory
- `SetSliderEnabled(slider, enabled)` — dims/enables a slider (used for height-slider sync dimming)
- `PLBeastOptionsFrame` — the named WoW frame (global, created by template)
- New `PLAYER_REGEN_ENABLED` event registration + handler branch in the existing `eventFrame`
- New locale keys: `L["PLBeast Options"]`, `L["Width"]`, `L["Height"]`, `L["Border Thickness"]`, `L["Lock position"]`, `L["Sync width & height"]`, `L["PLBeast. /plbeast | debug | reset"]`
- [05-02] `OpenBorderColorPicker()` wrapper, the border-color swatch button, `L["Border Color:"]`

<tasks>

<task type="auto">
  <name>Task 1: Add locale strings and combat-deferred open scaffolding</name>
  <files>PLBeast/Locales/enUS.lua, PLBeast/PLBeast.lua</files>
  <read_first>
    - PLBeast/Locales/enUS.lua (all 7 lines — current keys; note `Cannot open options in combat.` already exists)
    - PLBeast/PLBeast.lua lines 110-118 (forward declarations: `eventFrame`, `root`) — add `optionsFrame` and `pendingOptionsOpenAfterCombat` forward declarations near here
    - PLBeast/PLBeast.lua lines 627-735 (event frame, ADDON_LOADED, PLAYER_LOGIN block, event branches) — locate where to register `PLAYER_REGEN_ENABLED` and where to add its handler branch
    - PackLeaderHelper.lua lines 1021-1027 (combat-pending-open guard inside ToggleOptions)
    - PackLeaderHelper.lua lines 2623-2630 (PLAYER_REGEN_ENABLED deferred-open via C_Timer.After(0, ...))
  </read_first>
  <action>
    In enUS.lua, add new keys to the `PLBeastLocale` table (value identical to key, matching the existing self-identity convention): `PLBeast Options`, `Width`, `Height`, `Border Thickness`, `Lock position`, `Sync width & height`, and `PLBeast. /plbeast | debug | reset`. Do NOT re-add `Cannot open options in combat.` — it already exists. Leave the existing 5 keys intact.

    In PLBeast.lua, add two module-level forward declarations near the existing `local eventFrame` / `local root` block (~line 112-118): `local optionsFrame` and `local pendingOptionsOpenAfterCombat = false`.

    Register `PLAYER_REGEN_ENABLED` on `eventFrame` inside the `PLAYER_LOGIN` branch alongside the existing `eventFrame:RegisterEvent(...)` calls (~lines 713-717), per D-07 / UI-SPEC Frame Lifecycle Contract step 2.

    Add a `PLAYER_REGEN_ENABLED` handler branch to the `OnEvent` dispatcher (the elseif chain ending ~line 734). The branch: if `pendingOptionsOpenAfterCombat` is true, defer via `C_Timer.After(0, ...)` and inside the deferred closure re-check the flag is still true before calling `ToggleOptions()` (mirror PLH lines 2624-2629). Guard `C_Timer and C_Timer.After` before use.

    `ToggleOptions` is a forward-declared `local function` defined in Task 2; ensure the forward `local ToggleOptions` declaration exists before the event handler references it (declare alongside the other forward locals if the function body lands after the event block — the executor decides placement to keep upvalue resolution correct in Lua 5.1).
  </action>
  <verify>
    <automated>cd /home/cela/code_stuff/PLBeast && luac -p PLBeast/PLBeast.lua && luac -p PLBeast/Locales/enUS.lua && grep -q 'PLAYER_REGEN_ENABLED' PLBeast/PLBeast.lua && grep -q 'pendingOptionsOpenAfterCombat' PLBeast/PLBeast.lua && grep -q '"Sync width & height"' PLBeast/Locales/enUS.lua && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `PLBeast/Locales/enUS.lua` contains keys `Width`, `Height`, `Border Thickness`, `Lock position`, `Sync width & height`, `PLBeast Options`, and `PLBeast. /plbeast | debug | reset`; the 5 pre-existing keys are unchanged.
    - `PLBeast/PLBeast.lua` declares module-level `optionsFrame` and `pendingOptionsOpenAfterCombat` and registers `PLAYER_REGEN_ENABLED` on the existing `eventFrame` (no new event frame created).
    - The `OnEvent` dispatcher has a `PLAYER_REGEN_ENABLED` branch that defers an open via `C_Timer.After` only when `pendingOptionsOpenAfterCombat` is true, re-checking the flag inside the deferred closure.
    - If `luac` is unavailable in the environment, fall back to a Lua syntax sanity check the executor can run; flag any inability to byte-compile so it is caught at in-game `/reload`.
  </acceptance_criteria>
  <done>New locale keys present; combat-deferred open scaffolding wired into the existing event frame; file byte-compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Build ToggleOptions() frame with sliders + checkboxes and route the bare slash command</name>
  <files>PLBeast/PLBeast.lua</files>
  <read_first>
    - PLBeast/PLBeast.lua lines 520-566 (SetIconSize — sync mirroring, persists DB.width/height; ApplyIconSettings — applies size/position/border) — these are the live-apply setters every control binds to (D-11)
    - PLBeast/PLBeast.lua lines 14-26 (defaults: width=40, height=40, syncSize=true, borderThickness=1, locked=false)
    - PLBeast/PLBeast.lua lines 669-686 (current SlashCmdList["PLBEAST"] handler: debug | reset | else-help) — replace the `else` help branch so bare `msg == ""` opens the frame, unknown args print the new usage line
    - PLBeast/PLBeast.lua line 124 (Print) and line 718 (existing load message)
    - PackLeaderHelper.lua lines 552-627 (CreateSlider — copy and TRIM: remove the editBox block lines 579-610 and editBox refs in OnValueChanged 615-617; keep slider, Low/High/Text, valText, OnValueChanged, getValue seed)
    - PackLeaderHelper.lua lines 629-650 (CreateCheckbox — copy nearly verbatim)
    - PackLeaderHelper.lua lines 652-662 (SetSliderEnabled — copy, drop the editBox lines since PLBeast sliders have no editBox)
    - PackLeaderHelper.lua lines 1021-1048 (ToggleOptions frame creation: BasicFrameTemplateWithInset, SetSize, SetPoint CENTER, DIALOG strata, movable/drag, title FontString on TitleBg)
    - .planning/phases/05-configuration/05-UI-SPEC.md — Frame Architecture, Control Specifications, Spacing Scale (y-offsets), Interaction Contract, Slider/Checkbox Initialization (BINDING contract)
  </read_first>
  <action>
    Add trimmed `CreateSlider`, `CreateCheckbox`, and `SetSliderEnabled` local helpers (extract-and-trim from PLH per CONTEXT D and the "Established Patterns" extract convention). CreateSlider must NOT include the editBox (UI-SPEC: trimmed to keep the 280px-wide frame minimal); keep the `valText` readout (`GameFontHighlightSmall`, anchored `TOP, slider, BOTTOM, 0, -2`) per UI-SPEC Typography. Sliders are integer-valued: round in `OnValueChanged` and format `valText` as an integer string.

    Define `local function ToggleOptions()` implementing the UI-SPEC Interaction Contract:
    - Combat guard FIRST (CFG-04 / D-07): if `InCombatLockdown and InCombatLockdown()` then set `pendingOptionsOpenAfterCombat = true`, `Print(L["Cannot open options in combat."])`, and return. Otherwise clear the flag.
    - Lazy-create (D-05): if `optionsFrame` is nil, create `CreateFrame("Frame", "PLBeastOptionsFrame", UIParent, "BasicFrameTemplateWithInset")`, size 280×260, anchor `CENTER, UIParent, CENTER, 0, 0`, strata `DIALOG`, movable + `RegisterForDrag("LeftButton")` with `StartMoving`/`StopMovingOrSizing`, clamped to screen, and a title FontString (`GameFontHighlight`) centered on `optionsFrame.TitleBg` set to `L["PLBeast Options"]`.
    - Inside lazy-create, build the controls at the exact y-offsets from UI-SPEC "Control y-offsets": width slider -48, height slider -96, border-thickness slider -144, Lock position checkbox -228, Sync width & height checkbox -252. (The -200 color row is reserved for Plan 05-02; leave that vertical gap.)
      - Width slider: label `L["Width"]`, range 16-128 step 1, getValue `DB.width`, setValue calls `SetIconSize(value, DB.height)` (D-10, D-11).
      - Height slider: label `L["Height"]`, range 16-128 step 1, getValue `DB.height`, setValue calls `SetIconSize(DB.width, value)`. Store the height slider reference on the frame so the sync toggle can dim it.
      - Border-thickness slider: label `L["Border Thickness"]`, range 0-8 step 1, getValue `DB.borderThickness`, setValue writes `DB.borderThickness = value` then calls `ApplyIconSettings()` (D-10).
      - Lock position checkbox: label `L["Lock position"]`, getValue `DB.locked`, setValue writes `DB.locked = checked` (no setter call needed — drag scripts read DB.locked live). Maps directly, NO inversion (D-08).
      - Sync width & height checkbox: label `L["Sync width & height"]`, getValue `DB.syncSize ~= false` (default true, D-09), setValue writes `DB.syncSize = checked`, then calls `SetIconSize(DB.width, DB.height)` to re-mirror, then refreshes the height slider's dim state via `SetSliderEnabled(heightSlider, not DB.syncSize)`.
      - Apply initial height-slider dim state on creation: `SetSliderEnabled(heightSlider, not DB.syncSize)` so it starts dimmed when sync is on (UI-SPEC Slider dimming).
    - Toggle behavior (D-05): after create-or-reuse, if `optionsFrame:IsShown()` hide it, else show it.

    Replace the slash handler `else` branch (~line 683): bare `msg == ""` calls `ToggleOptions()`; any other unknown arg prints `L["PLBeast. /plbeast | debug | reset"]`. Keep `debug` and `reset` branches byte-for-byte unchanged (D-06).

    Do NOT add the color swatch or any ColorPicker call here — that is Plan 05-02. Do NOT introduce new DB keys, new defaults, or new apply logic; bind only to existing `SetIconSize`/`ApplyIconSettings` (D-11).
  </action>
  <verify>
    <automated>cd /home/cela/code_stuff/PLBeast && luac -p PLBeast/PLBeast.lua && grep -q 'PLBeastOptionsFrame' PLBeast/PLBeast.lua && grep -q 'BasicFrameTemplateWithInset' PLBeast/PLBeast.lua && grep -q 'local function ToggleOptions' PLBeast/PLBeast.lua && test "$(grep -c 'SetIconSize' PLBeast/PLBeast.lua)" -ge 3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `PLBeast.lua` contains a `local function ToggleOptions()` that builds a frame named `"PLBeastOptionsFrame"` from `"BasicFrameTemplateWithInset"`, sized 280×260, anchored CENTER, strata DIALOG.
    - ToggleOptions begins with an `InCombatLockdown()` guard that sets `pendingOptionsOpenAfterCombat = true`, prints `L["Cannot open options in combat."]`, and returns without creating/showing the frame.
    - The frame contains exactly three `OptionsSliderTemplate` sliders (width, height, border thickness) at y-offsets -48/-96/-144 and two `UICheckButtonTemplate` checkboxes (Lock position -228, Sync -252). No color swatch is present (reserved for 05-02).
    - Width slider setter calls `SetIconSize(value, DB.height)`; height setter calls `SetIconSize(DB.width, value)`; thickness setter writes `DB.borderThickness` then calls `ApplyIconSettings()`. No new apply logic is defined.
    - Lock checkbox writes `DB.locked = checked` with no inversion; Sync checkbox writes `DB.syncSize`, re-calls `SetIconSize(DB.width, DB.height)`, and re-dims the height slider via `SetSliderEnabled`.
    - Checkboxes seed from `DB.locked or false` and `DB.syncSize ~= false`; sliders seed from `DB.width/DB.height/DB.borderThickness` via their getValue on creation.
    - Bare `/plbeast` (`msg == ""`) calls `ToggleOptions()`; `debug` and `reset` subcommands are unchanged; unknown args print `L["PLBeast. /plbeast | debug | reset"]`.
    - CreateSlider has no editBox (trimmed); `luac -p` byte-compiles the file.
    - IN-GAME (manual, only proof — flagged): `/plbeast` opens the frame, `/plbeast` again hides it; `/plbeast` while in combat prints the combat message and the frame opens automatically when combat ends; dragging width/height sliders resizes the icon live; the thickness slider changes the border live; the Lock and Sync checkboxes reflect and change behavior live. Discharges Phase 4 carried-forward verification of independent width/height resize, sync toggle, and border thickness rendering + persistence across `/reload` (color/alpha portion completes in 05-02).
  </acceptance_criteria>
  <done>ToggleOptions builds the combat-guarded options window with 3 live sliders + 2 live checkboxes; bare /plbeast toggles it; subcommands intact; file byte-compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: In-game verification of options frame, sliders, toggles, combat guard, persistence</name>
  <action>HUMAN VERIFICATION ONLY — no code. Load the addon in the WoW client and perform the in-game checks in `<how-to-verify>`. This is the sole runtime/visual proof of CFG-01/02(sliders)/03/04 and discharges the Phase 4 carried-forward size + thickness verification. Do not proceed until the user approves.</action>
  <what-built>The `/plbeast` options frame with width/height/border-thickness sliders, Lock position and Sync width & height checkboxes, all live-applying to the icon, with combat-gated + deferred open.</what-built>
  <how-to-verify>
    1. Copy the PLBeast folder to the WoW AddOns directory (or symlink) and `/reload`.
    2. Type `/plbeast` out of combat — the options window appears centered. Type `/plbeast` again — it hides.
    3. Open it, drag the Width slider — the icon widens live. Drag Height — with Sync ON, height is dimmed/mirrors width; toggle Sync OFF, the Height slider enables and you can set a non-square size (e.g. 64×32). [Phase 4 carry-forward: confirm independent width/height renders, not a uniform square.]
    4. Drag Border Thickness to 3 — the border thickens live; set 0 — border disappears.
    5. Check Lock position — try to drag the icon; it should not move. Uncheck — drag works again.
    6. `/reload` and reopen `/plbeast` — sliders/checkboxes reflect the values you set (persistence). [Phase 4 carry-forward: confirm size + thickness persist.]
    7. Enter combat (e.g. target a dummy, start attacking), type `/plbeast` — it should NOT open and should print "Cannot open options in combat." Leave combat — the frame should open automatically.
    8. Confirm `/plbeast debug` and `/plbeast reset` still behave as before.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered/behaved incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player chat → slash handler | `/plbeast <arg>` free-text; already normalized via lower()+trim before exact-match routing |
| hand-edited SavedVariables → frame controls | `PLBeastDB` numeric/table fields may be corrupt; coercion guards run in ADDON_LOADED (existing) |
| combat lockdown → frame open | opening a frame during combat must be blocked (Blizzard lockdown + CFG-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | ToggleOptions during combat | mitigate | `InCombatLockdown()` guard returns before any CreateFrame/Show; deferred open on PLAYER_REGEN_ENABLED (D-07, CFG-04) |
| T-05-02 | Tampering | corrupt DB.width/height/borderThickness driving sliders | accept | Existing ADDON_LOADED tonumber() coercion (lines 656-660) normalizes before sliders read them; sliders also clamp to min/max via OptionsSliderTemplate |
| T-05-03 | Elevation of Privilege | drag-lock bypass | mitigate | Lock checkbox writes DB.locked; existing OnDragStart already returns when DB.locked is true — no new code path to bypass |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — WoW addon, no external dependencies (CLAUDE.md constraint) |
</threat_model>

<verification>
- `luac -p PLBeast/PLBeast.lua` and `luac -p PLBeast/Locales/enUS.lua` byte-compile cleanly.
- Source greps: `PLBeastOptionsFrame`, `BasicFrameTemplateWithInset`, `local function ToggleOptions`, `PLAYER_REGEN_ENABLED`, `pendingOptionsOpenAfterCombat` all present.
- `debug` and `reset` slash branches unchanged from current behavior.
- IN-GAME (human checkpoint, sole proof for runtime/visual behavior): open/close toggle, combat-block + deferred open, live resize (independent W/H), live border thickness, lock/sync toggles, persistence across /reload. Discharges Phase 4 carried-forward size/thickness verification.
</verification>

<success_criteria>
- CFG-01: `/plbeast` registered and bare command opens/toggles the options frame.
- CFG-02 (slider portion): frame has width, height, and border-thickness sliders (color picker completes in 05-02).
- CFG-03: frame has a drag (Lock position) toggle and a width/height sync toggle, both seeded from and writing to DB.
- CFG-04: frame cannot open during combat lockdown; deferred to PLAYER_REGEN_ENABLED.
- D-11: all controls bind to existing SetIconSize/ApplyIconSettings — no new apply logic.
</success_criteria>

<output>
Create `.planning/phases/05-configuration/05-01-SUMMARY.md` when done.
</output>
