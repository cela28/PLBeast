---
phase: 06-release-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: false
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
user_setup: []

must_haves:
  truths:
    - "Pushing a v* tag to cela28/PLBeast triggers the GitHub Actions release workflow"
    - "The release zip extracts to PLBeast/ at root (not nested)"
    - "The zip contains PLBeast.toc, PLBeast.lua, and Locales/enUS.lua — nothing else"
    - "The TOC inside the zip has Version matching the tag"
    - "A GitHub release entry exists with the zip attached and auto-generated notes"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered release workflow"
    - path: ".gitignore"
      provides: "Broader ignore coverage matching sibling addons"
  key_links:
    - from: "git tag v1.0.0"
      to: ".github/workflows/release.yml"
      via: "push tag event on v* pattern"
    - from: ".github/workflows/release.yml"
      to: "GitHub Releases"
      via: "gh release create with --generate-notes"
---

## Phase Goal

**As a** WoW player, **I want to** download PLBeast from GitHub Releases, **so that** I can install the addon by extracting the zip into my AddOns folder.

## Artifacts this phase produces

**Files created or modified:**
- `.gitignore` — expanded with broader coverage (`.DS_Store`, `.vscode/`, `*.bak`, `WTF/`, `Thumbs.db`)

**No new code symbols** — this phase is CI/CD plumbing only. The workflow and addon code already exist.

**Release artifacts (produced by GitHub Actions, not committed):**
- `PLBeast-1.0.0.zip` — release zip attached to GitHub release
- GitHub release entry at `cela28/PLBeast` tagged `v1.0.0`

<objective>
Publish the latest addon code from dev to main, expand .gitignore for broader coverage, push the v1.0.0 tag, and verify that the existing GitHub Actions release workflow produces a correctly structured zip with the addon inside.

This addresses all four REL requirements: REL-01 (workflow packages PLBeast/), REL-02 (tag-triggered), REL-03 (correct zip structure), REL-04 (remote origin). REL-04 is already satisfied; REL-01/REL-02 infrastructure exists on main and needs validation via D-09.

Purpose: Ship PLBeast v1.0.0 as a downloadable GitHub release — the final milestone deliverable.
Output: A GitHub release at cela28/PLBeast with a correctly structured PLBeast-1.0.0.zip attached.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-pipeline/06-CONTEXT.md

# Source files to publish
@PLBeast/PLBeast.lua
@PLBeast/PLBeast.toc
@PLBeast/Locales/enUS.lua

# Existing workflow (already on main — read to understand, do not recreate)
@.github/workflows/release.yml

# Current gitignore
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish latest addon code to main and expand .gitignore (D-08, D-06, REL-01, REL-03, REL-04)</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current state — only has Zone.Identifier)
    - .github/workflows/release.yml (already on main — verify it exists, do not recreate)
    - PLBeast/PLBeast.toc (verify Interface lines and file list are correct)
    - PLBeast/PLBeast.lua (the file being published — confirm it is the Phase 5.1 rewrite)
    - PLBeast/Locales/enUS.lua (the locale file — confirm "Rotation reset. Next: Boar." string)
    - .planning/phases/06-release-pipeline/06-CONTEXT.md (D-08 merge strategy, D-06 gitignore scope)
  </read_first>
  <action>
Per D-08: publish addon-relevant files to main via direct commits (not a merge from dev). The files that must land on main are ONLY: PLBeast/PLBeast.lua, PLBeast/PLBeast.toc, PLBeast/Locales/enUS.lua, .github/workflows/release.yml, .gitignore, and README.md. Files that MUST NOT be on main: .planning/, .claude/, CLAUDE.md.

Step 1 — Expand .gitignore per D-06 (Claude's discretion: yes, expand). On the dev branch, update .gitignore to match Duncedmaxxing's pattern. The file currently only has the Zone.Identifier entry. Add entries for: .DS_Store, Thumbs.db, .vscode/ directory, swap files (*.swp, *~), backup files (*.bak), and WTF/ directory. Keep the existing Zone.Identifier entry. Commit on dev.

Step 2 — Publish to main. Use git checkout of specific files from dev into a temporary main checkout. The approach: switch to main, then use git checkout dev -- for each addon-relevant file path (PLBeast/PLBeast.lua, PLBeast/PLBeast.toc, PLBeast/Locales/enUS.lua, .gitignore). Do NOT checkout .planning/, .claude/, or CLAUDE.md. Stage and commit as a single publish commit. Switch back to dev afterward.

Verify before committing to main: run git diff --cached --name-only to confirm ONLY addon-relevant files are staged. If any .planning/ or .claude/ or CLAUDE.md files appear, abort.

The commit message on main should be: "PLBeast v1.0.0 — Phase 5.1 engine rewrite + expanded gitignore"

After committing, push main to origin.
  </action>
  <verify>
    <automated>git log origin/main --oneline -1 | grep -q "v1.0.0" && echo "main updated" || echo "FAIL: main not updated"</automated>
    <automated>git show origin/main:.gitignore | grep -q "DS_Store" && echo "gitignore expanded" || echo "FAIL: gitignore not expanded"</automated>
    <automated>git ls-tree -r --name-only origin/main | sort</automated>
    <automated>git show origin/main:.planning/STATE.md 2>/dev/null && echo "FAIL: .planning on main" || echo "OK: .planning not on main"</automated>
  </verify>
  <acceptance_criteria>
    - git log origin/main --oneline -1 contains "v1.0.0" in the message
    - git ls-tree -r --name-only origin/main lists exactly: .github/workflows/release.yml, .gitignore, PLBeast/Locales/enUS.lua, PLBeast/PLBeast.lua, PLBeast/PLBeast.toc, README.md — nothing else
    - git show origin/main:.gitignore contains .DS_Store, .vscode/, *.bak, WTF/, *:Zone.Identifier
    - git show origin/main:.planning/STATE.md exits non-zero (file does not exist on main)
    - git show origin/main:CLAUDE.md exits non-zero (file does not exist on main)
    - Current branch after task is dev (switched back)
  </acceptance_criteria>
  <done>Main branch at origin contains the latest addon code from dev (Phase 5.1 engine rewrite), expanded .gitignore, and no dev-only files (.planning, .claude, CLAUDE.md). Ready for tagging.</done>
</task>

<task type="auto">
  <name>Task 2: Push v1.0.0 tag and verify release workflow (D-07, D-09, REL-01, REL-02, REL-03)</name>
  <files></files>
  <read_first>
    - .github/workflows/release.yml (understand the workflow steps: sed TOC version, zip -r, gh release create)
    - PLBeast/PLBeast.toc (confirm current Version line format for sed compatibility: "## Version: 0.2.4")
    - .planning/phases/06-release-pipeline/06-CONTEXT.md (D-07 first release is v1.0.0, D-09 validate by pushing tag)
  </read_first>
  <action>
Per D-07: the first release is v1.0.0. Per D-09: validate by actually pushing the tag.

Step 1 — Create and push the v1.0.0 tag on main. The tag must point at the latest main commit (the publish commit from Task 1). Create an annotated tag: git tag -a v1.0.0 -m "PLBeast v1.0.0" on main. Push the tag: git push origin v1.0.0.

Step 2 — Wait for the GitHub Actions workflow to complete. Use gh run list --workflow=release.yml --limit=1 to find the run, then gh run watch to wait for completion. The workflow triggers on push of v* tags.

Step 3 — Verify the release. Use gh release view v1.0.0 to confirm: the release exists, has title "PLBeast v1.0.0", and has the PLBeast-1.0.0.zip asset attached. Download the zip and verify its internal structure: unzip -l should show paths starting with PLBeast/ (PLBeast/PLBeast.toc, PLBeast/PLBeast.lua, PLBeast/Locales/enUS.lua). Verify no .planning/, .claude/, CLAUDE.md, or PackLeaderHelper files are in the zip.

Step 4 — Verify TOC version injection. Extract the zip to a temp directory, read PLBeast/PLBeast.toc, confirm the Version line reads "## Version: 1.0.0" (sed-injected from tag, per D-03).
  </action>
  <verify>
    <automated>gh release view v1.0.0 --repo cela28/PLBeast --json tagName,name,assets -q '.tagName' | grep -q "v1.0.0" && echo "release exists" || echo "FAIL: no release"</automated>
    <automated>gh release view v1.0.0 --repo cela28/PLBeast --json assets -q '.assets[].name' | grep -q "PLBeast-1.0.0.zip" && echo "zip attached" || echo "FAIL: no zip"</automated>
  </verify>
  <acceptance_criteria>
    - gh release view v1.0.0 shows tagName=v1.0.0 and name contains "PLBeast"
    - gh release view v1.0.0 assets list includes PLBeast-1.0.0.zip
    - Downloaded zip unzip -l shows entries starting with PLBeast/ (PLBeast/PLBeast.toc, PLBeast/PLBeast.lua, PLBeast/Locales/enUS.lua)
    - No entries in zip contain .planning, .claude, CLAUDE.md, or PackLeaderHelper
    - Extracted PLBeast/PLBeast.toc contains the line "## Version: 1.0.0" (sed-injected)
    - gh run list --workflow=release.yml shows the latest run with status "completed" and conclusion "success"
  </acceptance_criteria>
  <done>v1.0.0 tag pushed, GitHub Actions workflow ran successfully, release zip is correctly structured (PLBeast/ at root, TOC version is 1.0.0), and the release entry exists on GitHub with auto-generated notes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify v1.0.0 release on GitHub</name>
  <what-built>
PLBeast v1.0.0 GitHub release — the complete release pipeline from code publish to downloadable zip.

What was done:
1. Latest addon code (Phase 5.1 engine rewrite) published to main branch
2. .gitignore expanded with broader coverage
3. v1.0.0 tag pushed, triggering the release workflow
4. GitHub Actions produced and attached PLBeast-1.0.0.zip to the release
  </what-built>
  <how-to-verify>
1. Visit https://github.com/cela28/PLBeast/releases/tag/v1.0.0
2. Confirm the release exists with title containing "PLBeast v1.0.0"
3. Download PLBeast-1.0.0.zip
4. Extract the zip — confirm it creates a PLBeast/ folder (not nested further)
5. Inside PLBeast/: confirm PLBeast.toc, PLBeast.lua, Locales/enUS.lua are present
6. Open PLBeast.toc — confirm "## Version: 1.0.0" (not 0.2.4)
7. (Optional) Copy PLBeast/ into your WoW AddOns directory and verify it loads without errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → main | Addon code crosses from development to public-facing branch |
| main → GitHub Actions | Workflow executes on push event in a runner environment |
| GitHub Actions → Release | Zip artifact published as downloadable release |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | dev→main publish | medium | mitigate | Verify git ls-tree on main shows ONLY addon-relevant files before pushing; automated check in Task 1 verify block confirms no .planning/.claude/CLAUDE.md on main |
| T-06-02 | Information Disclosure | release zip contents | medium | mitigate | Workflow zips only PLBeast/ directory (zip -r PLBeast); Task 2 verify confirms no dev-only files in zip via unzip -l inspection |
| T-06-03 | Tampering | release.yml workflow | low | accept | Workflow already exists on main from a prior commit (af2c235); no modifications needed this phase. GitHub Actions permissions scoped to contents:write only. Risk is low — the workflow is simple and auditable. |
| T-06-04 | Denial of Service | GitHub Actions runner | low | accept | Single workflow on tag push only; no recurring triggers. GitHub-hosted runner with standard resource limits. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package manager installs in this phase — pure git/gh CLI operations and GitHub Actions with pinned actions/checkout@v4. |
</threat_model>

<verification>
1. Main branch contains ONLY: .github/workflows/release.yml, .gitignore, PLBeast/Locales/enUS.lua, PLBeast/PLBeast.lua, PLBeast/PLBeast.toc, README.md
2. v1.0.0 tag exists on origin pointing to latest main commit
3. GitHub Actions release workflow completed successfully
4. PLBeast-1.0.0.zip is attached to the v1.0.0 release
5. Zip internal structure: PLBeast/ at root with PLBeast.toc, PLBeast.lua, Locales/enUS.lua
6. PLBeast.toc inside zip has "## Version: 1.0.0"
7. Human verified the release page and zip contents
</verification>

<success_criteria>
- A WoW player can visit https://github.com/cela28/PLBeast/releases, download PLBeast-1.0.0.zip, extract it into their AddOns folder, and have a working PLBeast addon
- The release was created automatically by pushing a tag — no manual packaging
- The zip structure is correct for WoW addon installation (PLBeast/ at root)
- The TOC version matches the release tag (1.0.0)
</success_criteria>

<output>
Create `.planning/phases/06-release-pipeline/06-01-SUMMARY.md` when done
</output>
