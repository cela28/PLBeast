---
phase: "05.1"
plan_id: "05.1-01"
title: "Replace 10Hz timer poll with AzortharionUI's per-frame self-correcting prediction"
wave: 1
depends_on: []
files_modified:
  - PLBeast/PLBeast.lua
requirements:
  - PERF-01
autonomous: true
---

# Plan 05.1-01: Event-Driven Self-Correcting Rotation Tracking

<objective>
Replace PLBeast's 10Hz `C_Timer.NewTicker` poll with AzortharionUI's per-frame OnUpdate model and self-correcting prediction logic. The addon must behaviorally match AzortharionUI's `PollPackLeader()` step-for-step: pin prediction to the ready beast on appearance, advance on consumption, persist via SavedVariables, reset to boar on spec change, no login/boss-pull reset. All changes target `PLBeast/PLBeast.lua`.
</objective>

## Tasks

<task id="05.1-01-01">
<title>Add Pack Leader state variables and expand spell tracking constants</title>
<read_first>
- PLBeast/PLBeast.lua (current state variables at lines 30–109)
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-RESEARCH.md § "Area 4: State Variables & Data Tables"
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-CONTEXT.md § D-06, D-08
</read_first>
<action>
In the "Module-Level State Variables" section of PLBeast.lua, add these new state variables after the existing ones:

- `local plPhase = "off"` — three-state machine: "off" | "ticking" | "ready"
- `local plBeast = nil` — beast data table when ready buff active, nil otherwise
- `local plHasCdBuff = false` — Pack Leader countdown/parent buff active flag
- `local plDirty = true` — change flag (set true on any state transition)
- `local plCdDurObj = nil` — countdown duration object (for future use)
- `local plReadyDurObj = nil` — ready buff duration object (for future use)
- `local lastPolledTime = -1` — GetTime() throttle for OnUpdate handler

In the "Spell ID Constants" section, add:
- `local PL_COUNTDOWN = 471877` — Pack Leader countdown spell (Azor line 31)

Expand `TRACKED_SPELL_IDS` to include `SPELL_HOTPL_PARENT` (471876, already defined as a constant) and `PL_COUNTDOWN` (471877):
```
TRACKED_SPELL_IDS[SPELL_HOTPL_PARENT] = true
TRACKED_SPELL_IDS[PL_COUNTDOWN] = true
```

Change the default `nextBeastId` from `"wyvern"` to `"boar"` (D-08: Azor default is boar, line 53).
</action>
<acceptance_criteria>
- PLBeast.lua contains `local plPhase = "off"`
- PLBeast.lua contains `local plBeast = nil`
- PLBeast.lua contains `local plHasCdBuff = false`
- PLBeast.lua contains `local plDirty = true`
- PLBeast.lua contains `local PL_COUNTDOWN = 471877`
- PLBeast.lua contains `local lastPolledTime = -1`
- `TRACKED_SPELL_IDS[SPELL_HOTPL_PARENT]` is set to true
- `TRACKED_SPELL_IDS[PL_COUNTDOWN]` is set to true
- `nextBeastId` default value is `"boar"` (not `"wyvern"`)
</acceptance_criteria>
</task>

<task id="05.1-01-02">
<title>Add MatchTrackedSpell, TryCache, and ScanDeep CDM helper functions</title>
<read_first>
- PLBeast/PLBeast.lua (current CDM functions at lines 215–322)
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-RESEARCH.md § "CDM Cache: BuildCDMCache()" and "Architecture Mapping"
</read_first>
<action>
Add three new functions in the CDM section, above the existing `BuildCDMCache`:

1. `MatchTrackedSpell(info)` — takes a cooldownInfo table, returns the tracked spellID if `info.spellID`, `info.overrideSpellID`, or any `info.linkedSpellIDs[i]` is in `TRACKED_SPELL_IDS`; returns nil otherwise. Matches Azor lines 77–89.

2. `TryCache(frame)` — takes a CDM frame, reads its cooldownID via `frame.cooldownID or (frame.cooldownInfo and frame.cooldownInfo.cooldownID)`, calls `C_CooldownViewer.GetCooldownViewerCooldownInfo(cooldownID)`, passes result to `MatchTrackedSpell`, and if matched stores `cdmCache[matched] = { cdmFrame = frame, cooldownID = cooldownID }`. Matches Azor lines 91–101.

3. `ScanDeep(frame, depth)` — recursive DFS over CDM frame children up to depth 3. Calls `TryCache(frame)`, then iterates children via `frame:GetChildren()` and recurses. Matches Azor lines 103–112.

Also add a `GetFrameCooldownID(frame)` utility that returns `frame.cooldownID or (frame.cooldownInfo and frame.cooldownInfo.cooldownID)` — used by both `TryCache` and `FindCDMFrameByCooldownID`.

Update `CDM_FRAME_ROOT_NAMES` to use the same list as Azor: `{ "BuffBarCooldownViewer", "BuffIconCooldownViewer", "CDMGroups_Buffs" }` (PLBeast already has this).

Replace `CDM_CATEGORY_INDEXES = { 0, 1, 2, 3, 4, 5 }` with Azor's enum-based `CDM_CATS`:
```
local CDM_CATS
do
    local cats = Enum and Enum.CooldownViewerCategory
    if cats then
        CDM_CATS = { cats.Essential, cats.Utility, cats.TrackedBuff, cats.TrackedBar }
    end
end
```
</action>
<acceptance_criteria>
- PLBeast.lua contains `local function MatchTrackedSpell(info)` that checks spellID, overrideSpellID, and linkedSpellIDs
- PLBeast.lua contains `local function TryCache(frame)` that calls MatchTrackedSpell and stores to cdmCache
- PLBeast.lua contains `local function ScanDeep(frame, depth)` with depth cap at 3
- PLBeast.lua contains `local function GetFrameCooldownID(frame)` utility
- PLBeast.lua contains `local CDM_CATS` with Enum.CooldownViewerCategory entries
- `CDM_CATEGORY_INDEXES` table no longer exists in the file
</acceptance_criteria>
</task>

<task id="05.1-01-03">
<title>Add CDMFrameHasAura lazy re-resolve (D-05) and ScheduleRebuild debounced timer (D-03)</title>
<read_first>
- PLBeast/PLBeast.lua (current FindCDMFrameByCooldownID at lines 222–241, EnsureCacheFramesResolved at lines 338–346)
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-RESEARCH.md § "Lazy Re-Resolve: CDMFrameHasAura()" and "Debounced Rebuild: ScheduleRebuild()"
</read_first>
<action>
1. Rewrite `FindCDMFrameByCooldownID` to search ALL `CDM_FRAME_ROOT_NAMES` (not just `CDMGroups_Buffs`). Add a recursive `SearchFrameForCooldownID(frame, targetID, depth)` with depth cap 4 that checks `GetFrameCooldownID(frame) == targetID`. The outer function iterates all root names and returns the first match. Matches Azor lines 179–201.

2. Add `CDMFrameHasAura(data)` — the lazy re-resolve function. If `data.cdmFrame` is nil but `data.cooldownID` exists, call `FindCDMFrameByCooldownID(data.cooldownID)` to fill it. Then check `data.cdmFrame.auraInstanceID` is a positive number. Returns `(isActive: bool, instId: number|nil)`. Matches Azor lines 203–214.

3. Add `ScheduleRebuild()` — cancels any pending `rebuildTimer` and schedules `C_Timer.NewTimer(0.5, BuildCDMCache)`. Uses a module-level `local rebuildTimer` variable. Matches Azor lines 171–177.
</action>
<acceptance_criteria>
- PLBeast.lua contains `local function CDMFrameHasAura(data)` that returns two values (active, instId)
- CDMFrameHasAura calls FindCDMFrameByCooldownID when data.cdmFrame is nil and data.cooldownID is set
- CDMFrameHasAura checks `type(instId) == "number" and instId > 0`
- PLBeast.lua contains `local function ScheduleRebuild()` using `C_Timer.NewTimer(0.5, BuildCDMCache)`
- PLBeast.lua contains `local rebuildTimer` module-level variable
- FindCDMFrameByCooldownID searches all 3 CDM_FRAME_ROOT_NAMES (not just CDMGroups_Buffs)
- PLBeast.lua contains `local function SearchFrameForCooldownID(frame, targetID, depth)` with depth cap 4
</acceptance_criteria>
</task>

<task id="05.1-01-04">
<title>Rewrite BuildCDMCache to use Azor's ScanDeep + category scan pattern</title>
<read_first>
- PLBeast/PLBeast.lua (current BuildCDMCache at lines 309–322, GatherCooldownsFromFrameTree at lines 270–292, GatherCooldownsFromViewerCategories at lines 295–305)
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-RESEARCH.md § "CDM Cache: BuildCDMCache()"
</read_first>
<action>
Replace the current `BuildCDMCache` with Azor's two-phase pattern (Azor lines 137–169):

Phase 1 — Frame tree scan: iterate `CDM_FRAME_ROOT_NAMES`, call `ScanDeep(frame, 0)` on each root. This replaces the BFS `GatherCooldownsFromFrameTree`.

Phase 2 — Category set scan: iterate `CDM_CATS` (4 entries), call `C_CooldownViewer.GetCooldownViewerCategorySet(cat, true)` for each, then for each cooldownID call `GetCooldownViewerCooldownInfo(cooldownID)`, pass to `MatchTrackedSpell`, and store `cdmCache[matched] = { cdmFrame = nil, cooldownID = cdID }` (only if not already in cache — category entries lack frame refs). This replaces `GatherCooldownsFromViewerCategories`.

After both phases, also update `isPackLeaderActive` check: set a module-level boolean based on `(isBeastMastery or isSurvival) and IsPlayerSpell(SPELL_HOTPL_PARENT)`.

Set `cdmCacheBuilt = true` at the end.

Remove these now-dead functions:
- `RegisterDiscoveredCooldown()`
- `GatherCooldownsFromFrameTree()` 
- `GatherCooldownsFromViewerCategories()`
- `ReadSpellFromCooldownID()`
</action>
<acceptance_criteria>
- BuildCDMCache uses `ScanDeep` for frame tree scanning (not BFS queue)
- BuildCDMCache uses `CDM_CATS` for category scanning (not `CDM_CATEGORY_INDEXES`)
- BuildCDMCache uses `MatchTrackedSpell` for spell matching (not `ReadSpellFromCooldownID`)
- `RegisterDiscoveredCooldown` function does not exist in the file
- `GatherCooldownsFromFrameTree` function does not exist in the file
- `GatherCooldownsFromViewerCategories` function does not exist in the file
- `ReadSpellFromCooldownID` function does not exist in the file
- `cdmCacheBuilt` is set to `true` at the end of BuildCDMCache
</acceptance_criteria>
</task>

<task id="05.1-01-05">
<title>Write PollPackLeader — the core self-correcting prediction function</title>
<read_first>
- PLBeast/PLBeast.lua (current CheckAuraState at lines 377–439)
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-RESEARCH.md § "Self-Correcting Prediction: PollPackLeader()"
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-CONTEXT.md § D-06, D-07
</read_first>
<action>
Add a new `PollPackLeader()` function matching Azor lines 245–326. This replaces `CheckAuraState()`.

The function must:

1. Early exit if not `isPackLeaderActive` or not `cdmCacheBuilt`.

2. Save previous state: `hadCd = plHasCdBuff`, `hadReady = (plPhase == "ready")`, `oldBeast = plBeast`.

3. Read countdown buff state: check `cdmCache[SPELL_HOTPL_PARENT]` and `cdmCache[PL_COUNTDOWN]` via `CDMFrameHasAura()`. If either is active, `nowCd = true`.

4. Read beast ready state: iterate `{ {spell=SPELL_READY_BOAR, id="boar"}, {spell=SPELL_READY_BEAR, id="bear"}, {spell=SPELL_READY_WYVERN, id="wyvern"} }`. For each, check `cdmCache[beast.spell]` via `CDMFrameHasAura()`. First active hit sets `nowReady = true`, `nowBeast = beastData`. Break on first match.

5. Transition logic:
   - `nowReady and not hadReady`: Set `plPhase = "ready"`, `plBeast = nowBeast`, `plNextBeastId = nowBeast.id` (D-06: pin to ready beast), `plHasCdBuff = false`. Call `SaveState()`. Set `changed = true`.
   - `nowReady and hadReady`: Update `plBeast = nowBeast` only.
   - `not nowReady and hadReady`: Set `plPhase = nowCd and "ticking" or "off"`, `plBeast = nil`, `plNextBeastId = oldBeast and NEXT_BEAST[oldBeast.id] or "boar"`. Call `SaveState()`. Set `changed = true`.
   - `nowCd ~= hadCd`: Update `plHasCdBuff = nowCd`. If nowCd and plPhase == "off", set plPhase = "ticking". Set `changed = true`.
   - `plPhase == "ticking" and not nowCd and not nowReady`: Set plPhase = "off". Set `changed = true`.
   - If `changed`, set `plDirty = true`.

6. Update icon texture: after PollPackLeader runs, if `plDirty` is true, call `SetNextBeastId(plNextBeastId)` (which updates the icon texture) and reset `plDirty = false`. Alternatively, update the icon directly using `root.tex:SetTexture(ICON_FILE_BY_ID[plNextBeastId])` and set `plDirty = false`.

Note: The beast iteration order must be boar → bear → wyvern (matching Azor's `PL_BEASTS` array order), NOT the wyvern → boar → bear order from PLBeast's old code. Create a local `PL_BEASTS` array similar to Azor's to control scan order.
</action>
<acceptance_criteria>
- PLBeast.lua contains `local function PollPackLeader()` 
- PollPackLeader early-returns when `not isPackLeaderActive` or `not cdmCacheBuilt`
- PollPackLeader reads countdown buff via CDMFrameHasAura on `SPELL_HOTPL_PARENT` and `PL_COUNTDOWN` cache entries
- PollPackLeader iterates beast ready buffs in boar → bear → wyvern order (Azor's scan order)
- On ready appearance (`nowReady and not hadReady`): sets `plNextBeastId = nowBeast.id` (pin to ready beast)
- On consumption (`not nowReady and hadReady`): sets `plNextBeastId = NEXT_BEAST[oldBeast.id]` (advance)
- Calls SaveState() after each prediction change
- Icon texture updates when prediction changes
</acceptance_criteria>
</task>

<task id="05.1-01-06">
<title>Add SaveState/RestoreState and migrate persistence from integer to string</title>
<read_first>
- PLBeast/PLBeast.lua (current ADDON_LOADED handler at lines 828–867, SetNextBeastId at lines 158–166, NormalizeNextIndex at lines 150–154)
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-RESEARCH.md § "Persistence: SaveState/RestoreState" and "Area 7: Persistence Migration"
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-CONTEXT.md § D-09
</read_first>
<action>
1. Add `SaveState()` function: writes `DB.plNextBeastId = plNextBeastId` (string-based, matches Azor lines 221–225). Also keeps `DB.nextIndex` updated for backward compat: `DB.nextIndex = INDEX_BY_ID[plNextBeastId] or 2` (2 = boar default).

2. Add `RestoreState()` function: reads `DB.plNextBeastId` and validates via `NEXT_BEAST[saved]` (matches Azor lines 227–234). On success, sets module-level `plNextBeastId = saved`. Falls back to `ID_BY_INDEX[DB.nextIndex]` if no string value saved (migration path from pre-5.1 saves).

3. Update `ADDON_LOADED` handler: replace the `NormalizeNextIndex() + nextBeastId = ID_BY_INDEX[DB.nextIndex]` block with a call to `RestoreState()`. The string `DB.plNextBeastId` is the primary persistence key; integer `DB.nextIndex` is the fallback for migration.

4. Update `defaults` table: add `plNextBeastId = "boar"` to defaults. Change `nextIndex = 1` to `nextIndex = 2` (boar) so the legacy fallback also defaults to boar.

5. Update `SetNextBeastId(beastId)`: change the fallback from `"wyvern"` to `"boar"`. Keep the `DB.nextIndex` write for backward compat but also write `DB.plNextBeastId = nextBeastId`.
</action>
<acceptance_criteria>
- PLBeast.lua contains `local function SaveState()` that writes `DB.plNextBeastId`
- PLBeast.lua contains `local function RestoreState()` that reads `DB.plNextBeastId` with NEXT_BEAST validation
- RestoreState falls back to `ID_BY_INDEX[DB.nextIndex]` when `DB.plNextBeastId` is nil
- ADDON_LOADED calls RestoreState() instead of NormalizeNextIndex + ID_BY_INDEX lookup
- defaults table contains `plNextBeastId = "boar"`
- defaults table contains `nextIndex = 2` (boar, not 1/wyvern)
- SetNextBeastId fallback is `"boar"` not `"wyvern"`
- SetNextBeastId writes both `DB.plNextBeastId` and `DB.nextIndex`
</acceptance_criteria>
</task>

<task id="05.1-01-07">
<title>Wire OnUpdate handler and update event registrations</title>
<read_first>
- PLBeast/PLBeast.lua (current RefreshVisibility at lines 491–520, event handler at lines 820–975)
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-RESEARCH.md § "Area 5: OnUpdate Handler" and "Area 6: Event Registration Changes"
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-CONTEXT.md § D-01, D-03, D-04
</read_first>
<action>
1. Add OnUpdate handler function:
```
local function OnUpdateHandler(self, elapsed)
    local now = GetTime()
    if now == lastPolledTime then return end
    lastPolledTime = now
    PollPackLeader()
end
```

2. Update `RefreshVisibility()`:
   - When `isPackLeaderActive` becomes true (activation path): replace `StartPollTicker()` with `root:SetScript("OnUpdate", OnUpdateHandler)`. Keep `BuildCDMCache()` and `SeedAuraSnapshot()` calls (seed snapshot will be removed in T-08 when CheckAuraState is removed, but for now keep it to avoid breaking during the transition).
   - When `isPackLeaderActive` becomes false (deactivation path): replace `StopPollTicker()` with `root:SetScript("OnUpdate", nil)`.
   - Remove `eventFrame:RegisterEvent("UNIT_AURA")` and `eventFrame:UnregisterEvent("UNIT_AURA")` — UNIT_AURA is no longer the detection trigger.

3. Add new CDM event registrations in `PLAYER_LOGIN`:
   - `eventFrame:RegisterEvent("COOLDOWN_VIEWER_DATA_LOADED")`
   - `eventFrame:RegisterEvent("COOLDOWN_VIEWER_SPELL_OVERRIDE_UPDATED")`
   - `eventFrame:RegisterEvent("PLAYER_REGEN_DISABLED")`

4. Add event handling in the OnEvent script:
   - `COOLDOWN_VIEWER_DATA_LOADED` → call `ScheduleRebuild()`
   - `COOLDOWN_VIEWER_SPELL_OVERRIDE_UPDATED` → call `ScheduleRebuild()`
   - `PLAYER_REGEN_DISABLED` → if `not next(cdmCache)` then `BuildCDMCache()` (combat-enter fallback, Azor line 453)

5. Replace existing `CooldownViewerSettings.OnDataChanged` callback (lines 904–915) with a simpler call to `ScheduleRebuild()` (which already handles the 0.5s debounce).

6. Update spec change handling: in the existing talent/spec event handler block, add spec-change reset logic. When `ACTIVE_PLAYER_SPECIALIZATION_CHANGED` fires, after `QueueVisibilityRefresh()`, also reset: `plNextBeastId = "boar"`, `SaveState()`, and call a `ClearPackLeaderState()` function that sets `plPhase = "off"`, `plBeast = nil`, `plHasCdBuff = false`, `plDirty = true`.
</action>
<acceptance_criteria>
- PLBeast.lua contains `local function OnUpdateHandler(self, elapsed)` with GetTime() throttle
- RefreshVisibility sets `root:SetScript("OnUpdate", OnUpdateHandler)` on activation (no StartPollTicker)
- RefreshVisibility sets `root:SetScript("OnUpdate", nil)` on deactivation (no StopPollTicker)
- No `eventFrame:RegisterEvent("UNIT_AURA")` or `UnregisterEvent("UNIT_AURA")` calls in RefreshVisibility
- `COOLDOWN_VIEWER_DATA_LOADED` event is registered in PLAYER_LOGIN
- `COOLDOWN_VIEWER_SPELL_OVERRIDE_UPDATED` event is registered in PLAYER_LOGIN
- `PLAYER_REGEN_DISABLED` event is registered in PLAYER_LOGIN
- OnEvent handler for `COOLDOWN_VIEWER_DATA_LOADED` calls ScheduleRebuild()
- OnEvent handler for `COOLDOWN_VIEWER_SPELL_OVERRIDE_UPDATED` calls ScheduleRebuild()
- OnEvent handler for `PLAYER_REGEN_DISABLED` calls BuildCDMCache() when cache is empty
- CooldownViewerSettings.OnDataChanged callback calls ScheduleRebuild()
- Spec change events trigger plNextBeastId reset to "boar" and SaveState()
</acceptance_criteria>
</task>

<task id="05.1-01-08">
<title>Remove dead code: timer poll, CheckAuraState, snapshot-diff, C_UnitAuras fallback, login/boss resets</title>
<read_first>
- PLBeast/PLBeast.lua (full file — need to identify all removal targets)
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-RESEARCH.md § "Area 1: Timer Poll Removal", "Area 2: CheckAuraState Replacement"
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-CONTEXT.md § D-01, D-02, D-09
</read_first>
<action>
Remove the following dead code:

**Timer poll (Area 1):**
- `local pollTicker = nil` (line 101)
- `local POLL_INTERVAL = 0.1` (line 102)
- `local CDM_REBUILD_COOLDOWN = 1.0` (line 108)
- `local lastCDMRebuildAt = 0` (line 109)
- `StartPollTicker()` function (lines 474–480)
- `StopPollTicker()` function (lines 482–487)

**CheckAuraState and supporting functions (Area 2):**
- `local prevReady = { wyvern = false, boar = false, bear = false }` (line 90)
- `CheckAuraState()` function (lines 377–439)
- `SyncNextFromAddedReady(addedBeasts, startTimes)` function (lines 170–185)
- `SeedAuraSnapshot()` function (lines 444–448)
- Call to `SeedAuraSnapshot()` in RefreshVisibility (inside the activation path)

**C_UnitAuras fallback functions:**
- `IsReadyViaAura(spellId)` function (lines 358–363)
- `IsReadyBuffActive(spellId)` function (lines 367–372)
- `IsReadyViaCDM(spellId)` function (lines 350–354)

**CDM stale-frame helpers (replaced by CDMFrameHasAura lazy re-resolve):**
- `FrameStillOwnsCooldown(frame, cooldownID)` function (lines 332–336)
- `EnsureCacheFramesResolved()` function (lines 338–346)

**Login/boss-pull resets (D-09):**
- Remove `PLAYER_ENTERING_WORLD` isInitialLogin → `ResetRotationToWyvern()` block (lines 957–965). Keep the event registration but make the handler a no-op or remove it entirely.
- Remove `ENCOUNTER_START` → `ResetRotationToWyvern()` block (lines 967–974). Unregister `ENCOUNTER_START` event.
- Remove `ResetRotationToWyvern()` function (lines 202–205) if no longer called from slash command.
- Remove the `eventFrame:RegisterEvent("ENCOUNTER_START")` at line 825.

**UNIT_AURA handler:**
- Remove the `UNIT_AURA` event handler block (lines 927–931)
</action>
<acceptance_criteria>
- grep for `POLL_INTERVAL` returns 0 matches
- grep for `pollTicker` returns 0 matches
- grep for `StartPollTicker` returns 0 matches
- grep for `StopPollTicker` returns 0 matches
- grep for `CDM_REBUILD_COOLDOWN` returns 0 matches
- grep for `CheckAuraState` returns 0 matches
- grep for `prevReady` returns 0 matches
- grep for `SyncNextFromAddedReady` returns 0 matches
- grep for `SeedAuraSnapshot` returns 0 matches
- grep for `IsReadyViaAura` returns 0 matches
- grep for `IsReadyBuffActive` returns 0 matches
- grep for `IsReadyViaCDM` returns 0 matches
- grep for `FrameStillOwnsCooldown` returns 0 matches
- grep for `EnsureCacheFramesResolved` returns 0 matches
- grep for `ENCOUNTER_START` returns 0 matches
- grep for `ResetRotationToWyvern` returns 0 matches (unless kept for /plbeast reset)
- No `UNIT_AURA` event handler block exists
</acceptance_criteria>
</task>

<task id="05.1-01-09">
<title>Update slash commands, debug output, and NormalizeNextIndex for new state model</title>
<read_first>
- PLBeast/PLBeast.lua (slash command handler at lines 871–889, dprint calls throughout)
- .planning/phases/05.1-event-driven-rotation-tracking-drop-10hz-poll/05.1-CONTEXT.md § D-08, D-10
</read_first>
<action>
1. Update `/plbeast reset` handler: change from `SetNextBeastId("wyvern")` to `SetNextBeastId("boar")`. Also clear PollPackLeader state: `plPhase = "off"`, `plBeast = nil`, `plHasCdBuff = false`, `plDirty = true`. Update message from `"Rotation reset. Next: Wyvern."` to `"Rotation reset. Next: Boar."`.

2. Update `/plbeast debug` or add inline debug to PollPackLeader: ensure dprint calls show the new state variables: `plPhase`, `plNextBeastId`, `plHasCdBuff`, `plBeast` (if set, show beast id).

3. Keep `NormalizeNextIndex()` but update it to work with the new defaults: if `DB.nextIndex` is nil, default to 2 (boar) instead of 1 (wyvern).

4. Update the `ResetAuraState` function (if still needed): the only remaining caller may be the slash command reset. If `ResetAuraState` is only used for clearing `prevReady`, and `prevReady` is removed, then `ResetAuraState` can be removed too. Check if any callers remain. If `/plbeast reset` needs to clear state, use the inline `ClearPackLeaderState()` function instead.

5. Remove `NormalizeNextIndex()` if it's no longer called from anywhere (RestoreState replaces the ADDON_LOADED usage, and SetNextBeastId handles clamping via `NEXT_BEAST` validation).
</action>
<acceptance_criteria>
- `/plbeast reset` sets nextBeastId to "boar" (not "wyvern")
- `/plbeast reset` print message says "Boar" not "Wyvern"
- `/plbeast reset` clears plPhase, plBeast, plHasCdBuff, sets plDirty = true
- dprint output includes plPhase and plNextBeastId state
- No dead functions remain (NormalizeNextIndex removed if unused, ResetAuraState removed if unused)
</acceptance_criteria>
</task>

## Verification

<verify>
### Source Assertions
1. `grep -c "C_Timer.NewTicker\|POLL_INTERVAL\|pollTicker\|StartPollTicker\|StopPollTicker" PLBeast/PLBeast.lua` returns `0`
2. `grep -c "PollPackLeader" PLBeast/PLBeast.lua` returns `>= 2` (definition + call site)
3. `grep -c "CDMFrameHasAura" PLBeast/PLBeast.lua` returns `>= 2` (definition + call sites in PollPackLeader)
4. `grep -c "ScheduleRebuild" PLBeast/PLBeast.lua` returns `>= 3` (definition + CDM event calls)
5. `grep -c "SaveState\|RestoreState" PLBeast/PLBeast.lua` returns `>= 4` (definitions + call sites)
6. `grep -c "ENCOUNTER_START\|ResetRotationToWyvern\|IsReadyViaAura\|CheckAuraState\|prevReady" PLBeast/PLBeast.lua` returns `0`
7. `grep "plNextBeastId.*boar" PLBeast/PLBeast.lua` returns matches (default is boar)
8. `grep "OnUpdateHandler\|OnUpdate.*OnUpdateHandler" PLBeast/PLBeast.lua` returns matches

### In-Game Verification (manual)
9. Load PLBeast addon on a BM or SV hunter with Pack Leader talent active
10. Verify icon appears and shows the next predicted beast
11. Trigger beast spawns — verify icon pins to ready beast, then advances on consumption
12. `/reload` — verify prediction persists (same beast shown)
13. Relog — verify prediction persists (not reset to wyvern)
14. Change spec to MM or non-hunter — verify icon hides and prediction resets to boar on return
15. Side-by-side with AzortharionUI: both addons show the same next beast at every step
16. AddonProfiler: verify PLBeast has no constant timer poll; idle CPU comparable to AzortharionUI
</verify>

## must_haves

```yaml
truths:
  - "No C_Timer.NewTicker, POLL_INTERVAL, or pollTicker exists in PLBeast.lua"
  - "PollPackLeader function exists and implements self-correcting prediction (pin on ready, advance on consumption)"
  - "D-05: CDMFrameHasAura function exists with lazy per-cooldownID re-resolve via FindCDMFrameByCooldownID (replaces rebuild-on-all-false self-heal)"
  - "ScheduleRebuild function exists with 0.5s debounced C_Timer.NewTimer"
  - "SaveState/RestoreState persist plNextBeastId as a string to PLBeastDB"
  - "Default prediction is boar (not wyvern)"
  - "Spec change resets prediction to boar via SaveState"
  - "No login reset and no boss-pull reset (ENCOUNTER_START handler removed)"
  - "OnUpdate handler with GetTime() throttle replaces the timer poll"
  - "Three CDM rebuild triggers registered: COOLDOWN_VIEWER_DATA_LOADED, COOLDOWN_VIEWER_SPELL_OVERRIDE_UPDATED, CooldownViewerSettings.OnDataChanged"
  - "PLAYER_REGEN_DISABLED builds CDM cache if empty (combat-enter fallback)"
  - "C_UnitAuras fallback removed — CDM is sole source of truth for beast ready buffs"
  - "In-game side-by-side with AzortharionUI: predictions match step-for-step through spawns, reload, relog, boss pull, and spec change"
```

## Artifacts this phase produces

### New Functions
- `PollPackLeader()` — self-correcting prediction engine (replaces CheckAuraState)
- `CDMFrameHasAura(data)` — lazy CDM frame re-resolve, returns (active, instId)
- `ScheduleRebuild()` — 0.5s debounced CDM cache rebuild
- `MatchTrackedSpell(info)` — spellID/overrideSpellID/linkedSpellIDs matching
- `TryCache(frame)` — CDM frame → cache entry via MatchTrackedSpell
- `ScanDeep(frame, depth)` — recursive DFS over CDM frame children
- `GetFrameCooldownID(frame)` — cooldownID extraction utility
- `SearchFrameForCooldownID(frame, targetID, depth)` — recursive frame search by cooldownID
- `SaveState()` — persist plNextBeastId to PLBeastDB
- `RestoreState()` — load plNextBeastId from PLBeastDB with migration fallback
- `OnUpdateHandler(self, elapsed)` — per-frame GetTime()-throttled PollPackLeader driver
- `ClearPackLeaderState()` — resets plPhase/plBeast/plHasCdBuff/plDirty

### New State Variables
- `plPhase` — "off" | "ticking" | "ready" three-state machine
- `plBeast` — current ready beast data table
- `plHasCdBuff` — countdown buff active flag
- `plDirty` — state change flag
- `plCdDurObj` — countdown duration object
- `plReadyDurObj` — ready buff duration object
- `lastPolledTime` — GetTime() throttle for OnUpdate
- `rebuildTimer` — ScheduleRebuild timer handle

### New Constants
- `PL_COUNTDOWN = 471877` — Pack Leader countdown spell ID
- `CDM_CATS` — Enum.CooldownViewerCategory list (replaces CDM_CATEGORY_INDEXES)
- `PL_BEASTS` — beast data array in scan order (boar → bear → wyvern)

### New SavedVariables Key
- `PLBeastDB.plNextBeastId` — string-based prediction persistence

### Removed Symbols
- `pollTicker`, `POLL_INTERVAL`, `CDM_REBUILD_COOLDOWN`, `lastCDMRebuildAt`
- `StartPollTicker()`, `StopPollTicker()`
- `CheckAuraState()`, `prevReady`, `SyncNextFromAddedReady()`, `SeedAuraSnapshot()`
- `IsReadyViaCDM()`, `IsReadyViaAura()`, `IsReadyBuffActive()`
- `FrameStillOwnsCooldown()`, `EnsureCacheFramesResolved()`
- `RegisterDiscoveredCooldown()`, `GatherCooldownsFromFrameTree()`, `GatherCooldownsFromViewerCategories()`, `ReadSpellFromCooldownID()`
- `CDM_CATEGORY_INDEXES`
- `ResetRotationToWyvern()`, `NormalizeNextIndex()` (if no callers remain)
- `ResetAuraState()` (if no callers remain)
