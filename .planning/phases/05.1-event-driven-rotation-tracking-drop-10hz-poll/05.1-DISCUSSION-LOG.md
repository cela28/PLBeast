# Phase 05.1: Event-Driven Rotation Tracking - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-29
**Phase:** 05.1-event-driven-rotation-tracking-drop-10hz-poll
**Areas discussed:** Detection mechanism, CDM rebuild event scope, Prediction display semantics

---

## Detection Mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| Match Azor's CDM poll | OnUpdate once per frame with GetTime() throttle, CDM-only reads. Exact parity, proven approach. Still runs a per-frame check even when idle though. | ✓ |
| UNIT_AURA + CDM reads | Fire on UNIT_AURA event, read CDM frames for state. Zero cost when idle (truly event-driven). Potentially MORE efficient than Azor but slightly different trigger path. | |
| You decide | Let researcher/planner determine the best approach based on Azor's code + PLBeast's existing architecture. | |

**User's choice:** Match Azor's CDM poll
**Notes:** User explicitly confirmed "what we are doing is just copying the azorui beast implementation." The discovery that Azor doesn't use UNIT_AURA at all (contrary to the ROADMAP's original language) was a key finding from reading the actual Hunter.lua code. The user dropped the AzortharionUI zip into the repo for reference.

---

## CDM Rebuild Event Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Match Azor exactly | All 3 CDM events via 0.5s debounced ScheduleRebuild + BuildCDMCache on combat-enter if cache empty. Proven, resilient. | ✓ |
| You decide | Let researcher/planner determine the minimal set needed given PLBeast's architecture. | |

**User's choice:** Match Azor exactly
**Notes:** No additional discussion needed. Azor's approach is proven and the user wants exact parity.

---

## Prediction Display Semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Match Azor (self-correct) | Show ready beast during active phase, advance on consumption. Icon is always in sync with the game state. | ✓ |
| Keep current 'next after' semantic | Always show the beast that comes AFTER the current ready one. Would need extra logic on top of Azor's model. | |

**User's choice:** Match Azor (self-correct)
**Notes:** Confirmed the visible behavior change: during the ready phase, icon will show the ready beast itself (e.g., boar ready → boar icon) rather than the beast after it (current behavior: boar ready → bear icon).

---

## Rotation Order (clarification — not a discussion area)

During discussion, the rotation order was initially flagged as potentially different between PLBeast and Azor. User corrected this — both use `boar → bear → wyvern → boar`. The `NEXT_BEAST` table is identical.

---

## UNIT_AURA Debounce (dropped)

Originally selected as a discussion area, but became moot after the user chose "Match Azor's CDM poll" — Azor doesn't use `UNIT_AURA` at all, so there's nothing to debounce.

---

## Claude's Discretion

- Code restructuring approach (how to transform PLBeast's existing code to match Azor's model)
- Whether to keep `ID_BY_INDEX`/`INDEX_BY_ID` tables or simplify to Azor's string-based approach
- `TRACKED_SPELL_IDS` scope (Azor tracks more spells than PLBeast needs)
- OnUpdate handler wiring details

## Deferred Ideas

None — discussion stayed within phase scope. The reconsider-login-boss-reset todo was reviewed but not folded (explicitly deferred to post-5.1 evaluation).
